# 6A工作流规则
顺序:Align→Architect→Atomize→Approve→Automate→Assess
文档通信:Markdown文档

# 智能体配置
Align:AlgnAgent→/docs/align/
Architect:ArchAgent→/docs/design/
Atomize:AtomAgent→/docs/tasks/
Approve:AprvAgent→/docs/approve/
Automate:AutmAgent→/src、/tests、/docs/accept/
Assess:AsseAgent→/docs/assess/

# 状态管理
全局:/docs/status/GLOBAL_STATUS.md（工作流模式、当前任务、任务列表、整体进度）
任务:/docs/status/[任务名]_STATUS.md（阶段进度、审批状态、输出文档）
流转:大部分阶段自动流转，仅Approve阶段(manual模式)需审批
状态:待审批/已批准/已拒绝
问题:QUESTIONS/ANSWERS机制
歧义决策:智能体自主选择推荐方案（标注为"自动决策"），生成ANSWERS文档，不暂停工作流

# 工作流模式
模式:auto/manual（无默认值，必须明确指定）
模式优先级:用户指定 > 全局状态文档 > 任务状态文档 > 暂停提示
auto:所有阶段自动流转
manual:仅Approve阶段完成后需等待用户自然语言审批，其他阶段自动流转
校验输出:每个阶段启动时必须输出【模式校验通过：当前为[auto/manual]模式，来源：[用户指定/全局状态/任务状态]】

# 自然语言控制
自动:自动/快速/机器/自动审批
人工:人工/严格/手动审批
审批:审批通过/批准进入下一阶段

# 核心原则
阶段隔离、文档驱动、灵活审批、状态管理、安全保障
